{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "node scripts/hooks/session-start.js",
        "description": "Detect repo stack and load or build the DAILY skill set for this repo"
      }
    ]
  }
}
